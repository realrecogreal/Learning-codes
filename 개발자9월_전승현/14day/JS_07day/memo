함수를 정의만 하면 위에서 호출하든 아래서 호출을 하든 호이스팅때문에 맨위로 함수가 올라가서 어디서든 호출해도 상관이 없다.

중괄호 안에서 선언한 변수는 지역변수이기 때문에 밖에서 호출을 시도하여도 불러올 수 없다.

중복된 함수명을 정의하고 호출을 할시에 기존함수는 사라지고 마지막으로 정의한 함수를 호출한다.
변수도 재선언이 되면 마지막으로 저장된 값을 불러옴.

(function() {}); 즉시 실행함수 문법

